AWAAZ PUBLIC SITE – FIREBASE FIX

Use Live Server from this folder and open index.html.
The public feed queries Firestore by status="published" only and sorts by createdAt in the browser, so no composite Firestore index is required.
